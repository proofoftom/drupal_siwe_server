services:
  siwe_server.next_drupal_auth:
    class: Drupal\siwe_server\Service\NextDrupalAuthService

  siwe_server.cors_subscriber:
    class: Drupal\siwe_server\EventSubscriber\SiweServerCorsSubscriber
    arguments:
      - "@config.factory"
    tags:
      - { name: event_subscriber }